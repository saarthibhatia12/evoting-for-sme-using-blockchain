# Backend Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sme_voting
DB_USER=root
DB_PASSWORD=your_password_here

# Prisma Database URL for MySQL
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://root:your_password_here@localhost:3306/sme_voting"

# Blockchain Configuration
BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# Contract Addresses - set these after deploying the contracts
# Simple Voting contract (required)
CONTRACT_ADDRESS=your_deployed_voting_contract_address
# Quadratic Voting contract (optional - for quadratic voting features)
QUADRATIC_CONTRACT_ADDRESS=your_deployed_quadratic_contract_address

# Chain ID for local Hardhat network (default: 31337)
CHAIN_ID=31337
# Admin private key - Hardhat's first account (for development only!)
# WARNING: Never use this in production. Use a secure key management solution.
ADMIN_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h
